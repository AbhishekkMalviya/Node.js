// Declare the array named 'cityVisited' containing `"mumbai"` and `"sydney"`.
// Add `"Berlin"` to the array using the push method

let cityVisited = [`mumbai`, `sydney`, `ujjain`,`indore`,`jaipur`];

cityVisited.push(`Berlin`);

console.log(cityVisited);